var videoWidth;
var videoHeight;

var newStyles = document.createElement('style')
document.head.append(newStyles)
newStyles.innerHTML = ".trying {" +
  "top: " + videoHeight/2 + ";" +
  "left:"+ videoWidth * 0.8 +";" +
"}"
